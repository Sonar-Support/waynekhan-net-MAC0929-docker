---
services:
  sonarqube-2025-6-mcp:
    container_name: sonarqube-2025-6-mcp
    depends_on:
      - sonarqube-2025-6
    image: mcp/sonarqube:latest
    restart: unless-stopped
    environment:
      SONARQUBE_HTTP_HOST: 0.0.0.0
      SONARQUBE_HTTP_PORT: 8080
      SONARQUBE_TOKEN: ${SONAR_TOKEN}
      SONARQUBE_TRANSPORT: http
      SONARQUBE_URL: http://sonarqube-2025-6:9000
    ports:
      - 19019:8080

  sonarqube-2025-6:
    container_name: sonarqube-2025-6
    depends_on:
      - sonarqube-2025-6-postgres
    image: sonarqube:2025.6.1-enterprise
    environment:
      - SONAR_JDBC_URL=jdbc:postgresql://sonarqube-2025-6-postgres/sonarqube?currentSchema=public
      - SONAR_JDBC_USERNAME=sonar
    stop_grace_period: 3600s
    ports:
      - 19015:9000
    volumes:
      - ./conf:/opt/sonarqube/conf
      - ./data:/opt/sonarqube/data
      - ./extensions:/opt/sonarqube/extensions
      - ./logs:/opt/sonarqube/logs

  sonarqube-2025-6-postgres:
    container_name: sonarqube-2025-6-postgres
    image: postgres:17
    environment:
      - POSTGRES_DB=sonarqube
      - POSTGRES_HOST_AUTH_METHOD=trust
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=sonar
    volumes:
      - var-lib-postgresql-data:/var/lib/postgresql/data

volumes:
  var-lib-postgresql-data:
